import { ImageLoadHandlerDirective } from './image-load-handler.directive';

describe('ImageLoadHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
